---
- name: Example Playbook
  hosts: all
  gather_facts: false
  collections:
    - ansible.windows
    - community.windows
    - chocolatey.chocolatey
  # vars_files:
  #   - "{{ lookup('env','HOME') }}/ansible-secrets/windows/ansible_local_admin.yml"  # optional vault with secrets
  roles:
    - defos